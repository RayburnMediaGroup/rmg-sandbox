import WaitlistPage from "@/components/waitlist/WaitlistPage";

export const metadata = {
  title: "bandwidth — early access",
  description: "The artist platform built for how music actually works in 2026.",
};

export default function Home() {
  return <WaitlistPage />;
}
