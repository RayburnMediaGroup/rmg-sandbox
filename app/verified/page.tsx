import VerifiedPage from "@/components/waitlist/VerifiedPage";

export const metadata = {
  title: "bandwidth — you're confirmed",
};

export default function Verified() {
  return <VerifiedPage />;
}
